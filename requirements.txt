# miRBioGenAtlas — Python data-retrieval/processing pipeline
# Target: Python 3.10 (notebook executed on CPython 3.10.14)
# Versions match the manuscript Methods (requests 2.28, pandas 1.5).
# Used by: notebooks/notebook.ipynb
#
# Install:  pip install -r requirements.txt

requests==2.28.2
pandas==1.5.3
numpy==1.23.5

# Needed only to open/run the Jupyter notebook (not imported by the pipeline itself):
jupyter==1.0.0

# Standard-library modules also used (no install required): json, os, subprocess, time
